VITE_API_BASE_URL=/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional — only for non-browser / SSR fallbacks. In the browser, OAuth and
# emailRedirectTo always use window.location.origin (so Vercel previews work).
# Do NOT set this to localhost in Vercel env (it is baked at build time).
# VITE_APP_ORIGIN=
